# Round25 独立增量包

导入 `wenbo-round25-incremental.bundle`。必需基线为 `51497eacf5d370966e0a6d5b8d6f9f428ae1ce68`，包内分支 `improve/product-reliability`。之前各轮已在主线，不再导入旧完整包。

1. 保护本地修改，fetch origin，验证bundle及基线。将包内分支fetch到新的review分支，从最新origin/main建立整合分支后合并；不用旧首页覆盖更新的日报，不强推。
2. 阅读 `automation/PRODUCT_ROUND25.md` 和运行手册Round25节。此次包括移动首页密度/层级、数字化正文召回与近期新闻流、招聘目录直采及三条川大岗位。
3. 如主线已有9月13日之后的新数据，保留最新日报和原始发现；合并新增两条数字趋势内容及完整来源映射，不直接回滚digital-data.json。六条招聘review决策按signature合并，保留之后的新证据。川大三岗位如已收录则逐岗去重。
4. 哈希迁移只涉及9月13日复核输入的换行规范。原raw哈希及对应CRLF验证见审计文件，原始发现和选稿完全不变。若主线已迁移或有后续合法修订，按最新内容核对，不覆写。
5. 使用Python3.12或更新版本，运行全量测试、python build.py、最新日期项目校验、python automation/validate_product.py及git diff --check。云端本轮345项通过、90页校验通过。
6. 本地浏览器预览390px与1440px、浅色和深色，检查首页两行摘要/同行来源、标题进入全文、近期筛选、行业观察8条近期数字化报道、川大三个岗位。云端未完成截图验收；请把关键页面截图提供用户。颜色保持黑白灰与深蓝。
7. 依据当前会话的部署授权正常推送整合结果，禁止force push。之后每日仍运行数字化incremental及招聘发现；导入外部搜索结果时默认继续直采，--no-live只用于离线回放。

下一步：招聘附件和待核队列转化、数字化专题从发现池独立召回、连续多日端到端产出验证。不能把14个目录或33条线索当成全网覆盖/有效在招岗位。
